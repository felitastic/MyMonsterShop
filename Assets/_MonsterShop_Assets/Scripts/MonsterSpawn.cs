using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonsterSpawn : MonoBehaviour
{
    private void OnEnable()
    {
        //foreach(MonsterSlot slot in GameManager.Instance.CurMonsters)
        //{
        //    if (slot.CurMonster != null)
        //    {
        //        slot.SpawnCurrentMonster(slot.MonsterSpawn);
        //    }
        //    else if (slot.CurMonster == null)
        //    {
        //        slot.SetSymbol();
        //    }
        //}
    }
}
